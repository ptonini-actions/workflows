name: publish

on:
  push:
    branches:
      - main
      - master

permissions:
  contents: write

jobs:
  publish:
    uses: ptonini-actions/workflows/.github/workflows/publisher-github-action.yaml@main